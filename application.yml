eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
    
microservice:
  payment-service:
    endpoints:
      endpoint:
        uri: http://RAILWAYPAYMENT/payment/payment
        
  paymentCheck-service:
    endpoints:
      endpoint:
        uri: http://PAYMENTCHECK/check
        
        
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: Aniket@1234
        url: jdbc:mysql://localhost:3306/railwayTicket
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
    profiles:
        active: local        
        
        
        
